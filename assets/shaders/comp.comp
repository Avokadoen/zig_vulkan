#version 450

layout (local_size_x = 16, local_size_y = 16) in;
// layout (binding = 0, rgba8) uniform image2D framebuffer;

void main() {	
    // If we can get a white image to render, then we are set for now ...
	vec4 res = vec4(1.0);
	// imageStore(framebuffer, ivec2(gl_GlobalInvocationID.xy), res);
}
